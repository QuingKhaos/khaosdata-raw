name: Extract

on:
  push:
    branches-ignore:
      - _template
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Extract raw data
    uses: QuingKhaos/khaosdata-raw/.github/workflows/extraction.yml@main
    secrets: inherit
    with:
      version: experimental
